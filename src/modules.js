const { people, peoplesAges } = require("./people");

console.log(people, peoplesAges);

// Object with a lot of information about the current operating system.
const os = require("os");

console.log(os.homedir(), os.platform());
