// The following code can be run in the terminal with node.
// To do so navigate to this directory in a terminal and run the command
// `node test` (or your file name) this will run the below code using node.

const username = "Luke";

console.log(`My name is ${username}`);

const sendGreetingMessage = (name) => {
  console.log(`Hello ${name}`);
};

sendGreetingMessage("John");
sendGreetingMessage("Kane");
